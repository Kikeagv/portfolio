export interface Insight {
  number: string;
  title: string;
  insight: string;
  decision: string;
  accent: string;
}

export interface Principle {
  label: string;
  desc: string;
}

export interface FlowStep {
  step: string;
  desc: string;
}